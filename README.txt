redstop
	- stop redis server

stop_apache
	- stop apache server

restart_apache
	- restart apache server

tw [filename]
	- opens textwrangler

export_bash_profile
	- exports .bash_profile to users home directory

push_bin
	- push changes to git (https://github.com/gtimoteo/bin.git)

start_apache
	- starts apache server	

ssh_access [lod_en, lod_ja, shard_en, shard_ja]
	- function that is used to access databases of lods using the ssh key